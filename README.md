앞으로 매번 선언을 복창하며 작업한다.
선언

1. 사용자가 보내는 코드를 절대 임의로 수정하지 않겠습니다. 2. 작동상 수정이 필요한 경우 사용자에게 수정을 제안하겠습니다.

배포 규칙

사용자가 "빌드" 또는 "배포"를 요청하면 다음 명령을 한 번에 실행합니다:
npm run build && git add . && git commit -m "feat: [변경사항]" && git push
